import TaskApp from './js/app.js';

const myApp = new TaskApp();
myApp.init();
// const log = document.getElementById('log');

// const output = document.getElementById('output');

/* function logKey(e) {
  // output.innerText += `${e.code}`;
  console.log(`${e.code}`);
} */

// document.addEventListener('keydown', logKey);
